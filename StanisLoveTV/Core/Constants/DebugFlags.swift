enum PlayerDebugFlags {
    /// Shows a technical playback stats overlay (bitrate, resolution, stalls, buffer health)
    /// in the top-right corner of the video player. Flip and rebuild to toggle.
    static let showStatsOverlay = true
}
